import Chatbot from "../components/Chatbot";

const Chat = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        width: "400px",
        height: "600px",
      }}
    >
      <Chatbot />
    </div>
  );
};

export default Chat;
